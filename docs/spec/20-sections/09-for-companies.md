# Section 09 — for companies (blue card on off‑white)

Same shell as `FOR sponsors`: section `css-fy3uqk css-2fmumq` on `#f6f5f2`, **header band**
`#0056a1` blue (`css-q46q7n`) → **body band** `rgba(0,86,161,0.4)` blue tint (`css-tm4jxk`).
Radius 32/20px.

## 9.1 Eyebrow + heading (blue band, light text)

Eyebrow pill bg **teal `#1ca9b0`** (`css-emccw1`), light `#f6f5f2` label:
```
for companies
```
Heading (Golos Black 60px/1.2/1.2px UC center, **light `#f6f5f2`** — `css-p5pzcn`+`css-mcnr9h`):
```
TEAM TRAINING
```
Sub‑paragraph (Golos Medium 20px center, **light `#f6f5f2`** — `css-9j145o`+`css-wp9y4g`):
```
Vibe Coding workshops tailored to empower your enterprise team
```

## 9.2 Two cards (blue‑tint band)

Row `css-2acloa` (desktop `row; justify:center; align:center; gap:40px`); tablet `css-7cqj53`
(`gap:24px`); mobile stacked column. Each card `css-cf43y` (`flex:1; column; align:center;
gap:20px`): a **240×245** image (`css-jk7nsm`, `object-fit:cover`) + title + body.

**Card 1** — image `/_assets/v11/7ccee39211d353e9dcd86461550118140b82b885.png`
(source has trailing space in `team-building `):
```
team-building 
B2B training, consulting, and team-building centered on vibe coding
```
**Card 2** — image `/_assets/v11/c9655ebf623170cacf55920de14cefe3a44707c1.png`
```
Custom sessions
Custom sessions for enterprises looking to empower their teams
```
Title = Golos Black 32px UC `#181814` (`css-i0585y`); body = Golos Medium 20px `#181814`
(`css-9j145o`).

Then the light `contact for more` button (href `https://calendar.app.google/z9XuskPpZPvE5A5h7`,
`target="_blank"`).

## Responsive

2 cards across (1280 & 800) → stacked (375). Section radius 32px (≥800) / 20px (375).
